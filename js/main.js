document.addEventListener("DOMContentLoaded", () => {
  const clockTag = document.querySelector(".countdown");
  const clockHrsTag = document.querySelector(".countdown__hours");
  const clockMinsTag = document.querySelector(".countdown__minutes");
  const clockSecsTag = document.querySelector(".countdown__seconds");
  const clock = () => {
    /* initClock */
  };
});
